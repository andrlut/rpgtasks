{
  "name": "rpgtasks",
  "private": true,
  "version": "0.0.0",
  "description": "RPG Tasks — gamified habit tracker (Android-first)",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter app dev",
    "android": "pnpm --filter app android",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.33.2"
}
